[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"
watchPatterns = ["site/**", "client/**", "rust-server/**", "crates/**", "deploy/**", "Dockerfile"]

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
