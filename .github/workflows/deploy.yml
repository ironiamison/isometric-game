name: Deploy

on:
  push:
    branches: [master]
    paths:
      - 'client/**'
      - 'crates/aeven-protocol/**'
      - 'rust-server/**'
      - 'site/**'
      - 'Cargo.toml'
      - 'Cargo.lock'
      - 'rust-toolchain.toml'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to server
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: root
          key: ${{ secrets.SERVER_SSH_KEY }}
          script: /root/isometric-game/deploy.sh
